export default function Header() {
    return (
        <header class="mb-8 text-center">
            <h1 class="text-4xl font-bold text-gray-800 mb-2">Remote SQLite Viewer</h1>
            <p class="text-gray-600">Browse and explore remote SQLite databases</p>
        </header>
    );
}